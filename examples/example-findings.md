# Security Findings

_Generated by Mini Static Findings Scanner._

**Summary:** HIGH: 12 | MED: 11 | LOW: 3  (total 26)

**Languages: Python (15), JavaScript (7), Dotenv (2), Markdown (1), SQL (1)**

## HIGH (12)

**1. Hardcoded Secret**
`src/config.js:1` · CWE-798 · confidence 1.00

Possible hardcoded secret or credential found

**Fix:** Move secrets to environment variables or a managed secrets vault and rotate exposed values.

**2. Vulnerable Dependency**
`requirements.txt:2` · CWE-20 · confidence 0.90

flask 0.12.2 - CVE-2018-1000656: Flask is vulnerable to Denial of Service via incorrect encoding of JSON data

**Fix:** Upgrade flask to 0.12.3 or later.

**3. Vulnerable Dependency**
`requirements.txt:4` · CWE-502 · confidence 0.90

pyyaml 5.1 - CVE-2019-20477: Deserialization of Untrusted Data in PyYAML

**Fix:** Upgrade pyyaml to 5.2 or later.

**4. Disabled TLS Verification**
`src/insecure_examples.py:24` · CWE-295 · confidence 0.85

TLS/certificate verification appears disabled

**Fix:** Enable certificate verification; trust only valid CAs.

**5. Dangerous Code Execution**
`src/app.py:6` · CWE-95 · confidence 0.80

Dangerous dynamic code execution function detected

**Fix:** Avoid dynamic execution; if unavoidable, strictly validate and constrain inputs.

**6. Dangerous Code Execution**
`src/app.py:9` · CWE-95 · confidence 0.80

Dangerous dynamic code execution function detected

**Fix:** Avoid dynamic execution; if unavoidable, strictly validate and constrain inputs.

**7. Insecure Deserialization**
`src/insecure_examples.py:19` · CWE-502 · confidence 0.65

Unsafe deserialization of untrusted data

**Fix:** Use safe deserialization (e.g. yaml.safe_load, json) and never deserialize untrusted input.

**8. High Entropy Secret Context**
`.env:1` · CWE-798 · confidence 0.60

Long high-entropy value near a secret-related keyword

**Fix:** Review whether the value is a credential, token, or key; move real secrets to a vault.

**9. Path Traversal Risk**
`src/app.py:7` · CWE-22 · confidence 0.60

Unsafe file access from user-controlled input

**Fix:** Normalize paths and enforce an allow-listed base directory before file access.

**10. Path Traversal Risk**
`src/app.py:8` · CWE-22 · confidence 0.60

Unsafe file access from user-controlled input

**Fix:** Normalize paths and enforce an allow-listed base directory before file access.

**11. SQL Injection Risk**
`src/insecure_examples.py:29` · CWE-89 · confidence 0.60

SQL query appears built from string concatenation or interpolation

**Fix:** Use parameterized queries / prepared statements instead of string building.

**12. Hardcoded Secret**
`src/config.js:2` · CWE-798 · confidence 0.55

Possible hardcoded secret or credential found

**Fix:** Move secrets to environment variables or a managed secrets vault and rotate exposed values.

## MED (11)

**13. Vulnerable Dependency**
`package.json:6` · CWE-1333 · confidence 0.90

lodash 4.17.4 - CVE-2020-28500: Regular Expression Denial of Service (ReDoS) in lodash

**Fix:** Upgrade lodash to 4.17.21 or later.

**14. Vulnerable Dependency**
`package.json:7` · CWE-1321 · confidence 0.90

minimist 1.2.0 - CVE-2020-7598: Prototype Pollution in minimist

**Fix:** Upgrade minimist to 0.2.1 or later.

**15. Vulnerable Dependency**
`package.json:8` · CWE-1321 · confidence 0.90

jquery 3.3.1 - CVE-2019-11358: XSS in jQuery as used in Drupal, Backdrop CMS, and other products

**Fix:** Upgrade jquery to 3.4.0 or later.

**16. Vulnerable Dependency**
`requirements.txt:3` · CWE-522 · confidence 0.90

requests 2.19.1 - CVE-2024-47081: Requests vulnerable to .netrc credentials leak via malicious URLs

**Fix:** Upgrade requests to 2.32.4 or later.

**17. Sensitive File Present**
`.env:1` · CWE-552 · confidence 0.80

Sensitive configuration, credential, database, or backup file found

**Fix:** Keep sensitive files out of source control and public web roots.

**18. Sensitive File Present**
`backup.sql:1` · CWE-552 · confidence 0.80

Sensitive configuration, credential, database, or backup file found

**Fix:** Keep sensitive files out of source control and public web roots.

**19. Broad CORS Policy**
`src/server.js:2` · CWE-942 · confidence 0.80

Broad CORS policy allows requests from any origin

**Fix:** Restrict CORS origins to an explicit allow-list of trusted domains.

**20. Debug Mode Enabled**
`src/app.py:5` · CWE-489 · confidence 0.75

Debug or development mode appears enabled

**Fix:** Disable debug/development mode in production builds.

**21. Insecure HTTP URL**
`src/insecure_examples.py:38` · CWE-319 · confidence 0.55

Insecure HTTP URL detected instead of HTTPS

**Fix:** Use HTTPS for network communication.

**22. Weak Cryptography**
`src/insecure_examples.py:14` · CWE-327 · confidence 0.50

Weak or broken cryptographic algorithm used in a security context

**Fix:** Use modern algorithms (SHA-256+, bcrypt/argon2 for passwords, AES-GCM for ciphers).

**23. Sensitive Data in Logs**
`src/insecure_examples.py:35` · CWE-532 · confidence 0.50

Secret or credential appears to be written to logs/console

**Fix:** Never log secrets; redact sensitive fields before logging.

## LOW (3)

**24. Suspicious Security Comment**
`README.md:8` · CWE-546 · confidence 0.70

Security-related TODO, FIXME, or temporary bypass comment found

**Fix:** Resolve security-related temporary comments before release.

**25. Suspicious Security Comment**
`src/server.js:3` · CWE-546 · confidence 0.70

Security-related TODO, FIXME, or temporary bypass comment found

**Fix:** Resolve security-related temporary comments before release.

**26. Unpinned Dependency**
`requirements.txt:6` · CWE-1104 · confidence 0.40

Dependency 'certifi' is unpinned; cannot verify against advisories

**Fix:** Pin an exact version or commit a lockfile so it can be vulnerability-scanned.

